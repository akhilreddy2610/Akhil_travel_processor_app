_schema-version: "3.2"
ID: comnartproctravelprocessorapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comnartproctravelprocessorapp-destination-content
  type: com.sap.application.content
  requires:
  - name: comnartproctravelprocessorapp-destination-service
    parameters:
      content-target: true
  - name: comnartproctravelprocessorapp-repo-host
    parameters:
      service-key:
        name: comnartproctravelprocessorapp-repo-host-key
  - name: comnartproctravelprocessorapp-uaa
    parameters:
      service-key:
        name: comnartproctravelprocessorapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comnartproctravelprocessorapp_html_repo_host
          ServiceInstanceName: comnartproctravelprocessorapp-html5-service
          ServiceKeyName: comnartproctravelprocessorapp-repo-host-key
          sap.cloud.service: comnartproctravelprocessorapp
        - Authentication: OAuth2UserTokenExchange
          Name: comnartproctravelprocessorapp_uaa
          ServiceInstanceName: comnartproctravelprocessorapp-xsuaa-service
          ServiceKeyName: comnartproctravelprocessorapp-uaa-key
          sap.cloud.service: comnartproctravelprocessorapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comnartproctravelprocessorapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comnartproctravelprocessorapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comnartproctravelprocessorapp.zip
      name: comnartproctravelprocessorapp
      target-path: resources/
- name: comnartproctravelprocessorapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comnartproctravelprocessorapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comnartproctravelprocessorapp-destination-service
    service-plan: lite
- name: comnartproctravelprocessorapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comnartproctravelprocessorapp-xsuaa-service
    service-plan: application
- name: comnartproctravelprocessorapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comnartproctravelprocessorapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
